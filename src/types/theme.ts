import { Theme } from "simple-table-core";

/**
 * Extended theme type that includes custom themes beyond simple-table-core's built-in themes
 */
export type ThemeOption = Theme | "custom-light" | "custom-dark";
